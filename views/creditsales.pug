doctype html
head
  meta(charset='UTF-8')
  meta(name='viewport' content='width=device-width, initial-scale=1.0')
  title Credit Sales Form
  style.
    body {
    font-family: Arial, sans-serif;
    background-color: #ADD8E6; /* Blue-themed background */
    }
    .container {
    max-width: 500px;
    margin: 40px auto;
    padding: 20px;
    background-color: #fff;
    border: 1px solid #ddd;
    border-radius: 10px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }
    .form-group {
    margin-bottom: 20px;
    }
    label {
    display: block;
    margin-bottom: 10px;
    }
    input[type="text"], input[type="tel"], input[type="number"], input[type="date"] {
    width: 100%;
    height: 40px;
    margin-bottom: 20px;
    padding: 10px;
    border: 1px solid #ccc;
    border-radius: 5px;
    }
    button[type="submit"] {
    width: 100%;
    height: 40px;
    background-color: #4c78af;
    color: #fff;
    padding: 10px;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    }
    button[type="submit"]:hover {
    background-color: rgb(62, 69, 142);
    }
.container
  h2 Credit Sales Form
  form#credit-sales-form
    .form-group
      label(for='buyer-name') Name of Buyer:
      input#buyer-name(type='text' name='buyer-name' required='')
    .form-group
      label(for='national-id') National ID (NIN):
      input#national-id(type='text' name='national-id' required='')
    .form-group
      label(for='location') Location:
      input#location(type='text' name='location' required='')
    .form-group
      label(for='contacts') Contacts:
      input#contacts(type='tel' name='contacts' required='')
    .form-group
      label(for='amount-due') Amount Due (UGX):
      input#amount-due(type='number' name='amount-due' required='')
    .form-group
      label(for='sales-agent') Sales Agent Name:
      input#sales-agent(type='text' name='sales-agent' required='')
    .form-group
      label(for='due-date') Due Date:
      input#due-date(type='date' name='due-date' required='')
    .form-group
      label(for='produce-name') Produce Name:
      input#produce-name(type='text' name='produce-name' required='')
    .form-group
      label(for='produce-type') Type of Produce:
      input#produce-type(type='text' name='produce-type' required='')
    .form-group
      label(for='tonnage') Tonnage:
      input#tonnage(type='number' name='tonnage' required='')
    .form-group
      label(for='dispatch-date') Date of Dispatch:
      input#dispatch-date(type='date' name='dispatch-date' required='')
    button(type='submit') Submit
script.
  const form = document.getElementById('credit-sales-form');
  form.addEventListener'submit', (e) => 
  e.preventDefault();
  const buyerName = document.getElementById('buyer-name').value;
  const nationalId = document.getElementById('national-id').value;
  const location = document.getElementById('location').value;
  const contacts = document.getElementById('contacts').value;
